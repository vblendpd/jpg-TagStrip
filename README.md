Bare-bones command line utility to remove date-taken and program-used from JPG files.
Usage:
  jpg_tag_strip input.jpg [output.jpg]  //process a single file
  jpg_tag_strip --dir [folder]          //process every .jpg/.jpeg in folder (default: current dir)
